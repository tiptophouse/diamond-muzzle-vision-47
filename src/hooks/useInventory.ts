
// Unified inventory hook - this replaces useOptimizedInventory, useInventoryData, and useInventoryCrud
export { useUnifiedInventory as useInventory } from './useUnifiedInventory';

// Re-export for backward compatibility during migration
export { useUnifiedInventory } from './useUnifiedInventory';
