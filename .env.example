# Supabase Configuration (already configured)
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# API Configuration (if needed in the future)
# VITE_API_TOKEN="your-api-token-here"
# VITE_CUSTOM_API_KEY="your-custom-key"

# Note: Never commit the actual .env file with real secrets to git!
# This is just a template showing what environment variables are available.
